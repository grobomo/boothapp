# Root package.json + .env.example

## Goal
Create root package.json tying all boothapp components with scripts for start:watcher, test, demo, preflight. Create .env.example documenting all environment variables.

## Success Criteria
- [x] Root package.json with name, version, description, scripts
- [x] Scripts: start:watcher, test, demo, preflight
- [x] .env.example with all env vars (AWS_REGION, S3_BUCKET, ANALYSIS_MODEL, etc.)
- [x] Scripts actually work (dry-run validation)
